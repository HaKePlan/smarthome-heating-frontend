import Head from 'next/head';
import Layout from '../../components/layout';

export default function Alarm() {
  return (
    <Layout>
      <Head>
        <title>heating client | dashboard</title>
      </Head>
      <div>nothing to see here, only the dashboard</div>
    </Layout>
  );
}
